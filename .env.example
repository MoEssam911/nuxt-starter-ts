# PUBLIC CONFIGURATION (Safe to commit to Git)
# These variables are exposed to the client side. They must begin with NUXT_PUBLIC_
NUXT_PUBLIC_API_BASE=http://localhost:3000/api

# SECRETS (NEVER COMMIT TO GIT)
# These variables are only available server-side.
# NUXT_API_SECRET=your_local_secret_here
